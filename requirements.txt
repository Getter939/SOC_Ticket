# Core framework
Django==6.0.5
asgiref==3.11.1      # Django async support (required by Django)
sqlparse==0.5.5      # SQL formatting (required by Django)
tzdata==2026.2       # IANA timezone data (required by Django with USE_TZ=True)

# Database driver
psycopg2-binary==2.9.12

# Environment / config
python-decouple==3.8

# Static files serving (dev + production)
whitenoise==6.9.0

# WSGI server (production — Dockerfile CMD). Never run `runserver` in prod.
gunicorn==23.0.0

# Word report export (apps/incidents/reports.py → DOCX incident reports)
python-docx==1.2.0
lxml==6.1.1              # required by python-docx
typing_extensions==4.16.0 # required by python-docx

# PDF report export (apps/incidents/reports.py -> xhtml2pdf HTML-to-PDF)
xhtml2pdf==0.2.17
reportlab==4.5.1         # required by xhtml2pdf
pypdf==6.1.3             # required by xhtml2pdf + structural PDF validation in tests
Pillow==12.3.0           # required by reportlab/xhtml2pdf
html5lib==1.1            # required by xhtml2pdf
six==1.17.0              # required by html5lib
arabic-reshaper==3.0.1   # required by xhtml2pdf
python-bidi==0.6.11      # required by xhtml2pdf
pyHanko==0.35.2          # required by xhtml2pdf
pyhanko-certvalidator==0.31.1 # required by xhtml2pdf
asn1crypto==1.5.1        # required by pyHanko/pyhanko-certvalidator
cryptography==49.0.0     # required by pyHanko
cffi==2.1.0              # required by cryptography
pycparser==3.0           # required by cffi
oscrypto==1.3.0          # required by pyhanko-certvalidator
PyYAML==6.0.3            # required by pyHanko
svglib==2.0.2            # required by xhtml2pdf
cssselect2==0.9.0        # required by svglib
tinycss2==1.5.1          # required by svglib/cssselect2
webencodings==0.5.1      # required by html5lib/tinycss2
tzlocal==5.4.4           # required by pyHanko
uritools==6.1.2          # required by pyhanko-certvalidator

# HTTP client (apps/wazuh_ingest/ingest.py → OpenSearch alert ingestion)
requests==2.34.2

# Login brute-force protection / account lockout (DB-backed, so it holds
# across gunicorn workers). Wired in config/settings.py: INSTALLED_APPS, the
# AxesStandaloneBackend auth backend, and AxesMiddleware.
django-axes==7.1.0
