# ── Secrets / environment ─────────────────────────────────────────────────
# Never commit real credentials. Copy .env.example → .env and fill it in.
.env
.env.*
!.env.example
# Demo/staging account credentials — never track (kept locally for testers).
test_accounts.txt

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──────────────────────────────────────────────────
venv/
env/
.venv/

# ── Django ────────────────────────────────────────────────────────────────
*.sqlite3
staticfiles/
media/
config/settings_local.py

# ── Logs ──────────────────────────────────────────────────────────────────
*.log
logs/

# Backup output
backups/
*.dump
*.tar.gz.enc
*.tar.gz.gpg

# ── IDE ───────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── OS ────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
# LibreOffice/OpenOffice editor locks (e.g. .~lock.report_template_v1.docx#)
.~lock.*#
*.js

.codex_work/
.claude/skills/03-exploit-development/scripts/payload_generator.py
.claude/settings.json
/.claude
*.md
# Documentation that stays versioned. NOTE: `*.md` above is a blanket ignore,
# so any NEW .md file is silently untracked unless un-ignored here. Paths below
# were updated 2026-07-21 when docs/ was reorganised into category subfolders.
!docs/README.md
!docs/operations/production-deployment.md
!docs/architecture/workflow-change-log.md
# Deliberately NOT un-ignored: docs/uat/uat-*.md and
# docs/operations/grafana-wazuh-wall.md contain a shared UAT password and
# internal host details. Keep them local unless those are scrubbed first.
.claude/skills/03-exploit-development/scripts/payload_generator.py
.claude/skills/03-exploit-development/SKILL.md
